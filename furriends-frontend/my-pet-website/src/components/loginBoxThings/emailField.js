import { TextInput } from '@mantine/core';

export default function EmailField() {
    return (
        <TextInput
          variant="filled"
          label="Email address"
          placeholder="Email address"
        />
    );
}
